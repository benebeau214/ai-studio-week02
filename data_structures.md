# data_structures

다음 5개 시나리오 각각에 대해 ① 선택한 자료구조 ② 선택 근거(다른 후보를 배제한 이유 포함) ③ 3~5줄의 예시 코드를 담은 리포트를 data_structures.md 파일로 작성한다.

1. 상품코드를 입력하면 즉시 재고 수량을 확인해야 하는 재고 관리 데이터
    
    (1) 선택한 자료구조
    
    dictionary
    
    (2) 선택 근거
    
    dictionary 자료구조를 사용하면 상품코드를 key 값으로, 재고 수량을 value로 매핑해두면 상품코드를 입력시 바로 value 값인 재고 수량을 확인할 수 있기 때문이다.
    
    set이나 list는 상품코드와 재고 수량을 직접적으로 하나의 쌍으로 저장할 수 없기 때문에 즉시 확인해야하는 이 경우에는 적절하지 않다.
    
    tuple의 경우는 (상품코드, 재고 수량) 쌍을 지정할 수는 있지만 수정이 불가하기 때문에 변동 가능성이 있는 재고 수량을 저장하기에는 적절하지 않다.
    
    (3) 예시 코드
    
    ```python
    keys = ['001', '002', '003'] # 상품코드가 001, 002, 003
    values = [100, 120, 214] # 각 상품코드에 재고가 각각 100, 120, 214
    stock = dict(zip(keys, values)) # key : valude 형식인 dictionay 생성
    print(stock['001']) # 상품 코드가 001인 상품의 재고 수량을 확인
    ```
    
2. 이벤트 응모 고객 명단에서 중복 응모를 제거하고, 기존 회원 명단과의 교집합을 구해야 하는 상황
    
    (1) 선택한 자료구조
    
    set
    
    (2) 선택 근거
    
    set 자료구조는 중복을 제거해주는 기능과 교집합, 합집합과 같은 집합 연산 기능을 지원하기 때문에 이 경우에 제일 알맞다. 다른 자료구조들의 경우에는 중복 검사와 집합 연산을 하려면 별도의 코드 작성이 더 필요하기 때문에 알맞지 않다.
    
    (3) 예시 코드
    
    ```python
    # 이벤트 응모 고객 명단, set은 중복을 허용하지 않기 때문에 이름이 두 번 이상 들어간 고객 이름이 하나만 남게된다.
    event_customers = {'김리나', '박성현', '박도현', '김하진', '박성현', '조유나', '이현주', '이녹두', '백나현', '김리나'}
    # 기존 회원 명단
    existing_customers = {'김리나', '박성현', '한채희', '김아순'}
    # set이 지원하는 intersection 메소드를 통해 기존 회원과 이벤트 응모 고객의 교집합을 구한다.
    Intersection = event_customers.intersection(existing_customers)
    ```
    
3. 월별 매출액을 1월부터 12월까지 순서대로 저장하고 순회하는 데이터
    
    (1) 선택한 자료구조
    
    list
    
    (2) 선택 근거
    
    set을 제외한 list, tuple, dictionary가 순서가 있는 자료구조인데 
    
    dictionary는 주로 순서 보다는 key를 뽑아서 해당하는 key의 value를 조회할 때 적합하고, tuple은 한 번 저장하면 수정이 불가하기 때문에 당월의 매출액이 누적될 때마다 업데이트를 해야하거나 하는 경우가 있을 수도 있기 때문에 적합하지 않다고 본다. 그래서 list가 이 시나리오에 제일 적절한 자료구조라 생각한다.
    
    (3) 예시 코드
    
    ```python
    # 인덱스 0~11이 1~12월을 나타내고, 저장된 값들이 월별 매출액
    months_sales = [1200, 1700, 2400, 3700, 2680, 3410, 2900, 3100, 2370, 3500, 3290, 4000] 
    ```
    
4. 한 번 발급되면 절대 변경되어서는 안 되는 (위도, 경도) 매장 좌표
    
    (1) 선택한 자료구조
    
    tuple
    
    (3) 선택 근거
    
    tuple은 list, set, dictionary와 달리 한 번 저장 시 변경이 불가하기 때문에 이 시나리오에 제일 적합하다.
    
    (4) 예시 코드
    
    ```python
    # 매장의 (위도, 경도) 좌표
    store_coordinate = (37.5512, 126.9413)
    ```
    
5. 1,000만 줄짜리 웹 서버 접속 로그 파일에서 특정 조건의 줄 수를 세는 작업 (자료구조 + 처리 방식 관점에서 서술)
    
    (1) 선택한 자료구조 
    
    Iterator & Generator
    
    (2) 선택 근거
    
    대용량 파일 전체를 list에 한 번에 올리면 메모리를 초과해서 컴퓨터가 멈출 수 있다. Iterator와 Generator를 이용해 파일을 읽으면 파일을 처음부터 끝까지 한 번만 순회해 속도가 빠르고 메모리 사용량을 줄일 수 있다.
    
    (3) 예시 코드
    
    ```python
    # itergator와 generator를 사용해 특정 조건에 해당하는 줄 수를 세는 함수
    def count_log_lines(file_path, target):
    	with open(file_path, 'r', encoding='utf-8') as file:
    		count = sum(1 for line in file if target in line)
    	return count
    ```